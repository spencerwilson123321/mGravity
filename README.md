# mGravity
A simple bouncing ball with gravitational pull written in C++ using the SFML graphics library.

# Compile & Run (Linux)

Compile:

```
chmod +x build.sh;
./build.sh;
```

Run:

```
./main;
```